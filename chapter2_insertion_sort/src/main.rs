fn main() {

    let before_sort: [i32] = generating_array_to_sort();
    println!("before sort: {before_sort}");

    let after_sort = insert_sort(before_sort, items_to_sort);
    println!("after sort: {after_sort}");
}

fn generating_array_to_sort() -> [i32] {
    // some code

}

fn insert_sort(array: [i32], items_to_sort: i32) -> {
    for items in (0..items_to_sort) {

}
